代码块：一对{}
分类
构造代码块：直接定义在类中，且没有使用static修饰，
每创建一个对象，就由jvm自动调用一次，对象创建完成后，不能显式调用

静态代码块：直接定义在类中，使用static修饰，
只在类加载时，调用一次；只能调用静态成员

局部代码块：定义在方法或其他代码块中，代表一块作用域；上级作用域被调用，随之被调用

顺序:
静态代码块>main方法>构造代码块>构造方法

类的加载顺序
1.加载类本身
2.加载类本身的同时，加载静态成员（main）
3.加载普通成员
