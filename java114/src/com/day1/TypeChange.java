package com.day1;

public class TypeChange {
	public static void main(String[] args) {
		/*byte b = 1;
		int i = 129;
		//long l  = 1L;
		
		//类型转换：
		//自动类型转换（隐式类型转换）：小转大，自动转；类型安全的
		//java可以将取值范围小的类型自动转化为取值范围大的类型
		i = b;//byte类型可以赋值给int类型的变量
		l = i;
		
		//java不支持取值范围大的类型自动转化为取值范围小的类型；可能损失精度
		//b = i;
		
		//强制类型转换（显式类型转换，造型）：大转小，强制转;不安全
		//调用者可以强制JVM将取值范围大的，转为取值范围小的
		b = (byte)i;
		
		System.out.println("b="+b);*/
		
		
		
		
		
	}
}
